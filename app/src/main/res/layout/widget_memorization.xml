<?xml version="1.0" encoding="utf-8"?><!-- 암송 구절 위젯. widget_today_verse와 같은 id(widget_root/title/text/label/message)를 갖고
     있어서 WidgetViews의 공통 함수를 그대로 쓴다. 위쪽 줄에 그룹·순번과 "다음" 버튼이 더 있다. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 배경 그림. 글자는 그대로 두고 배경만 투명하게 만들 수 있도록 별도 이미지뷰로 둔다. -->
    <ImageView
        android:id="@+id/widget_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitXY"
        android:importantForAccessibility="no"
        android:src="@drawable/bg_widget_light" />

    <LinearLayout
        android:id="@+id/widget_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="12dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="암송 구절"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="@color/widget_light_accent" />

            <TextView
                android:id="@+id/widget_group"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:maxLines="1"
                android:ellipsize="end"
                android:textSize="11sp"
                android:textColor="@color/widget_light_text_secondary" />

            <TextView
                android:id="@+id/widget_next"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingStart="8dp"
                android:paddingTop="2dp"
                android:paddingBottom="2dp"
                android:text="다음 ›"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="@color/widget_light_accent" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_label"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:maxLines="1"
            android:ellipsize="end"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="@color/widget_light_text_primary" />

        <TextView
            android:id="@+id/widget_text"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center_vertical"
            android:text="불러오는 중이에요"
            android:textSize="15sp"
            android:lineSpacingMultiplier="1.1"
            android:maxLines="12"
            android:ellipsize="end"
            android:textColor="@color/widget_light_text_primary" />

        <TextView
            android:id="@+id/widget_message"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:visibility="gone"
            android:textSize="13sp"
            android:textColor="@color/widget_light_text_secondary" />

    </LinearLayout>

</FrameLayout>